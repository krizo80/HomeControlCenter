1. Install pip
   sudo apt-get install python-pip python-dev build-essential

2. Install Flask (globally)
   sudo -H pip install flask

3. Install GPIO library
    a. Download package
	http://pypi.python.org/pypi/RPi.GPIO
    b. Install python-dev library
	sudo apt-get install python-dev
    c. Run setup file
	sudo python setup.py install
    d. Alternative to above is to use pip (still installing python-dev is required)
	pip install RPi.GPIO

5. Install smsapi library (https://github.com/smsapi/smsapi-python-client)
    a. sudo pip install smsapi-client

5. Run application (as a service)
      Add one of below entry to /etc/rc.local file

      1.cd /HomeControlCenter && python hcc.py &
      2.start-stop-daemon -b -S --chdir /HomeControlCenter -x hcc.py


TODO list:
1. ---------------czujnik szamba
2. ---------------settings
3. ---------------zraszacze raportuja status jak piec
4. radio zamien na player (nazwa klasy i pliku)
5. rozklad jazdy pkm dodaj tez regionalne
6. ---------------wysylanie zdarzen przez sms
7. wspolpraca z alarmem
8. kamera 
9. oddzielna klasa dla info 
11. Tryb autopodlewania (wykrywanie deszczu i zalaczanie zraszaczy wg konfiguracji)
12. usunac strone 'auth' - wykrywanie autoryzacji w '/'
13. hccDeamon dodac lapanie wyjatkow i wazdym wywolaniu timerEvent - dodac klase logowanie wyjatkow (wyswietlana w SystemInfo)
14. zapisywanie aktualnej historii - w wykresach pieca dodac klawisz 'zapisz'
15. ---------------piec nie powinien zapisywac statusu - robi to ActionClass